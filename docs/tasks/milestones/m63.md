# Milestone 63 — Map Scale, Connectivity, and Landmarks

**Goal:** Make room maps feel larger and more connected, and ensure each room exposes its own landmark in gameplay UI.

**Status:** ✅ COMPLETED

## Scope Delivered

1. Added per-room `landmark` metadata to room schema/runtime model.
2. Expanded all room grids under `assets/regions/*/rooms/*.toml` (35 files) by adding interior playable space without breaking existing coordinates.
3. Enhanced world-map utility/widget to expose and render:
   - current room landmark
   - intra-region travel links (room-to-room travel triggers)
4. Updated docs for room format/schema and world map widget behavior.
5. Validated with `cargo check` and visual scenario runs.

**Done when:**
- [x] Each room has a landmark shown in world UI.
- [x] Maps are expanded across all region room files.
- [x] World map UI shows room landmark + local path connectivity.
- [x] Visual testing scenarios run successfully after changes.
