# Milestone 66 — Border Exits Graph for Room Traversal

**Goal:** Make border-based room transitions independent from door/trigger placement by introducing explicit directional room exits.

**Status:** ✅ COMPLETED

## Scope Delivered

1. Added `[exits]` room schema (`north/east/south/west`) to define adjacent room graph.
2. Updated runtime room/data models to load directional exits.
3. Changed edge movement to use `[exits]` only (not trigger placement).
4. Restricted step-on-travel for in-region links (special trigger travel still supported for non-local routes).
5. Backfilled exits metadata in region room assets.
6. Updated traversal tests and docs.

**Done when:**
- [x] Border pushes transition by explicit directional exits.
- [x] Normal room flow no longer depends on door/`!` trigger placement.
- [x] Tests and visual scenarios pass.
