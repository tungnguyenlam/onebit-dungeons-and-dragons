# M26 — Second Region Content Pass

**Status:** 🚧 in progress  
**Depends on:** M21 (Region Navigation), M25 (Faction Simulation)

---

## Goal

Bring `emberpeak-summit` to full content parity with `valley-of-ash`: complete the room graph (adding Summit Crater), add random encounters, hook up the local faction (Emberpeak Dwarves), and add a primary region quest.

---

## Scope

- Expand `emberpeak-summit` rooms from 4 to 5.
- Add `emberpeak_dwarves` faction to all local NPCs.
- Add at least one hostile encounter (Ember Wraith).
- Add a local quest: "The Volcanic Curse".
- No new engine features (data-only pass).

---

## Target Files

- `assets/regions/emberpeak-summit/region.toml`
- `assets/regions/emberpeak-summit/rooms/summit_crater.toml`
- `assets/regions/emberpeak-summit/npcs/*.toml`
- `assets/regions/emberpeak-summit/dialog/*.toml`
- `assets/quests/side/volcanic_curse.toml`

---

## Done When

- [ ] `summit_crater.toml` exists and is linked in `region.toml`.
- [ ] `warden_brom` and `archivist_nyra` belong to `emberpeak_dwarves` faction.
- [ ] At least one `encounter` trigger exists in the region (Targeting `ember_wraith`).
- [ ] Quest `volcanic_curse` is accepted via `warden_brom` and has at least two stages.
- [ ] `cargo run -- --validate-assets` passes.

---

## Verification

```bash
cargo run -- --validate-assets
scripts/validate_assets.sh
```
